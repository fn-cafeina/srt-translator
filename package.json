{
  "name": "srt-translator",
  "version": "1.0.0",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html --public-url ./",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@parcel/core": "^2.13.3",
    "gh-pages": "^6.3.0",
    "parcel": "^2.13.3"
  }
}
